# Dockerfile Optimizer

## Explanation
Optimizes a Dockerfile for faster build times and smaller image sizes.

## Detailed Prompt
Copy and paste the prompt below:

```text
Review my Dockerfile for a [Node.js/Python/Go] application: [Paste Dockerfile]. Refactor it to utilize multi-stage builds, minimize the final image size, leverage layer caching efficiently, and explicitly run the container as a non-root user for security. Explain the optimizations you made.
```
