# Advanced Python Scripting

## Explanation
Claude is highly proficient at logical, well-structured Python coding.

## Detailed Prompt
Copy and paste the prompt below:

```text
Write a robust, production-ready Python script that scrapes the website [URL], extracts [Specific Data Points], and saves the data to a SQLite database. The code must include comprehensive error handling (try/except blocks), exponential backoff for rate limits, logging via the standard library, and type hints for all functions.
```
