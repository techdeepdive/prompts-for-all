# NPM/Yarn Dependency Conflict Resolver

## Explanation
Untangles the mess of peer dependencies and conflicting package versions.

## Detailed Prompt
Copy and paste the prompt below:

```text
I am trying to run 'npm install' but I am getting massive ERESOLVE peer dependency conflicts regarding [Package Name, e.g., React or Webpack]. Here is the error log: [Paste Log]. Should I use --legacy-peer-deps, use overrides/resolutions in package.json, or downgrade a specific package? Give me the safest path to fix this.
```
