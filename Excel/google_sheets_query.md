# Google Sheets QUERY function

## Explanation
Leverages the powerful SQL-like QUERY function in Sheets.

## Detailed Prompt
Copy and paste the prompt below:

```text
I am using Google Sheets instead of Excel. I have a database in tab 'RawData' spanning A:Z. I want to pull only columns A, C, and F into a new tab, but only where column B equals 'Active' and arrange it by column F descending. Write the precise =QUERY() formula string that will accomplish this.
```
